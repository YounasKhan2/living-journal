import { useState,type FormEvent } from 'react';import { ArrowRight } from 'phosphor-react';import { useDocumentTitle } from '../../hooks/useDocumentTitle';export function ContactScreen(){useDocumentTitle('Contact');const[sent,setSent]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();setSent(true)};return <main className="contact-page page-gutter page-top"><header className="page-intro"><span className="eyebrow">Contact</span><h1>Send a note.<br/><span className="serif">We read every one.</span></h1></header><div className="contact-layout"><div><p>Have a story tip, correction, partnership idea or something worth discussing?</p></div><form className="contact-form" onSubmit={submit}><label>Name<input required/></label><label>Email<input required type="email"/></label><label>Message<textarea required rows={6}/></label><button type="submit">Send message <ArrowRight size={16}/></button>{sent?<p role="status">Message captured in this demo.</p>:null}</form></div></main>}
