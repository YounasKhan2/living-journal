const signals=['Artificial intelligence','Software craft','Independent thinking','Business','Design','Startups','Career intelligence'];export function SignalTicker(){const repeated=[...signals,...signals];return <section className="signal-ticker" aria-label="Coverage areas"><div className="signal-ticker__track">{repeated.map((signal,index)=><span key={`${signal}-${index}`}><i/> {signal}</span>)}</div></section>}
