import { Clock } from 'phosphor-react';import { useEffect,useRef } from 'react';import { Link,useParams } from 'react-router-dom';import { useContent } from '../../context/ContentContext';import { useDocumentTitle } from '../../hooks/useDocumentTitle';import { ArticleBody } from './components/ArticleBody';import { RelatedStories } from './components/RelatedStories';export function StoryDetailScreen(){const{slug}=useParams();const{publishedPosts}=useContent();const post=publishedPosts.find(i=>i.slug===slug);const progress=useRef<HTMLDivElement>(null);useDocumentTitle(post?.title??'Story');useEffect(()=>{const update=()=>{const max=document.documentElement.scrollHeight-window.innerHeight;const value=max>0?window.scrollY/max:0;if(progress.current)progress.current.style.transform=`scaleX(${value})`};window.addEventListener('scroll',update,{passive:true});update();return()=>window.removeEventListener('scroll',update)},[]);if(!post)return <main className="empty-page page-gutter"><h1>This story is no longer available.</h1><Link to="/stories">Return to stories</Link></main>;const related=publishedPosts.filter(i=>i.id!==post.id&&i.category===post.category).slice(0,3);return <main className="story-detail"><div className="reading-progress" ref={progress}/><header className="story-detail__hero page-gutter page-top"><div className="story-detail__head"><span className="eyebrow">{post.category}</span><h1>{post.title}</h1><p>{post.dek}</p><div className="story-meta"><span>{post.date}</span><span><Clock size={15}/> {post.readTime}</span><span>By {post.author}</span></div></div><div className="story-detail__cover"><img src={post.image} alt=""/></div></header><ArticleBody sections={post.sections}/><RelatedStories posts={related.length?related:publishedPosts.filter(i=>i.id!==post.id).slice(0,3)}/></main>}
