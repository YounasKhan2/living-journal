import type { Post } from '../../../types/content';import { ArticleCard } from '../../../components/global/ArticleCard';import { SectionHeading } from '../../../components/global/SectionHeading';export function RelatedStories({posts}:{posts:Post[]}){if(!posts.length)return null;return <section className="related-stories page-gutter section-space"><SectionHeading eyebrow="Keep reading" title={<>Related <span className="serif">stories</span></>}/><div className="related-stories__grid">{posts.map(post=><ArticleCard key={post.id} post={post}/>)}</div></section>}
