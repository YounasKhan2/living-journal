import type { ArticleSection } from '../../../types/content';export function ArticleBody({sections}:{sections:ArticleSection[]}){return <article className="article-body">{sections.map((s,i)=>{if(s.type==='heading')return <h2 key={i}>{s.text}</h2>;if(s.type==='quote')return <blockquote key={i}>{s.text}</blockquote>;if(s.type==='image')return <figure key={i}><img src={s.src} alt={s.alt}/>{s.caption?<figcaption>{s.caption}</figcaption>:null}</figure>;return <p className={i===0?'lead':''} key={i}>{s.text}</p>})}</article>}
